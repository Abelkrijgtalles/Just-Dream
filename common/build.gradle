architectury {
    common("fabric", "forge", "neoforge")
}
loom { mixin { defaultRefmapName = "just_dream.refmap.json" } }

dependencies {
    modImplementation "net.fabricmc:fabric-loader:${rootProject.fabric}"
}
